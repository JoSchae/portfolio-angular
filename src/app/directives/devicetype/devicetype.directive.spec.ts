import { DevicetypeDirective } from './devicetype.directive';

describe('DevicetypeDirective', () => {
  it('should create an instance', () => {
    const directive = new DevicetypeDirective();
    expect(directive).toBeTruthy();
  });
});
